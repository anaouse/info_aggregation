完成！三个修改都已实现：

1. **AnimeItem hover 效果加强**：添加了更明显的阴影（`0 4px 12px`）、向上位移 4px（`translateY(-4px)`）、边框颜色变成主题色
2. **点击视频自动播放**：给 `ShowVideo` 添加了 `autoPlay` prop，点击视频后会自动播放。同时用 `key={videoUrl}` 确保切换视频时重新渲染 `<video>` 元素
3. **返回按钮样式优化**：改用文本 `← 返回`，添加了边框、合适的 padding，hover 时有边框和背景高亮效果，更符合你的设计风格

现在你可以刷新页面测试：
- 鼠标悬停在动漫卡片上会有明显的上浮 + 阴影效果
- 点击视频列表中的某一集，视频会自动开始播放
- 返回按钮现在是带边框的文本按钮，hover 有反馈