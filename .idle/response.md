完成！已经修改后端代码：

1. **导入 `sort` 包**
2. **`VideoFile` 结构体增加 `ModTime` 字段**（Unix 时间戳），用 `json:"-"` 标记不暴露给前端
3. **`scanVideosInFolder` 函数增加排序逻辑**：
   - 收集视频文件时记录 `ModTime`
   - 返回前按 `ModTime` 升序排序（越早创建越靠前）

现在接口返回的视频列表会按照文件创建时间从早到晚排序，前端展示的选集面板就会从第 1 集开始依次显示。你测试通过了就可以了。