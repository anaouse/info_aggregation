完成！现在的方案：

1. **`root.css` 恢复原样**，删除了所有全局 `html/body/#root` 重置，不影响其他页面
2. **`.anime-play-page` 使用 `position: fixed`**，完全脱离文档流，占满整个视口，不产生滚动条，也不影响其他页面布局

构建通过，现在：
- **AnimePlayPage**：固定全屏，header + 视频播放器占满视口，无滚动
- **其他页面**：保持原样，正常滚动和边距